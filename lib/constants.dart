import 'package:flutter/material.dart';

// Warna untuk jawaban yang benar
const Color correct = Color.fromARGB(255, 1, 230, 20);

// Warna untuk jawaban yang salah
const Color incorrect = Color.fromARGB(255, 228, 13, 13);

// Warna netral (digunakan untuk teks pada OptionCard)
const Color neutral = Color.fromARGB(255, 255, 255, 255);

// Warna latar belakang aplikasi
const Color background = Color.fromARGB(255, 137, 22, 82);
